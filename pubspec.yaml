name: loan_form_automation
description: Loan Form Automation System

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.7.0
  cloud_firestore: ^6.3.0
  firebase_storage: ^13.3.0

  # PDF
  pdf: ^3.10.7
  printing: ^5.12.0

  # Utilities
  intl: ^0.18.1
  uuid: ^4.3.3
  share_plus: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
